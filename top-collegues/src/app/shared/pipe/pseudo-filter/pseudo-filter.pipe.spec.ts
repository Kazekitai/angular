import { PseudoFilterPipe } from './pseudo-filter.pipe';

describe('PseudoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PseudoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
